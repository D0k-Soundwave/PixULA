node_modules/
test-results/
playwright-report/

# Contained/portable build of the app (a generated duplicate of index.html +
# css/ + js/). Regenerate with `npm run build:portable`; not tracked.
PixULA_Distilled/

# Bench inputs are the user's own images, not repo content
docs/bench-images/*
!docs/bench-images/README.md

# Stray tracked copy of the generated portable build (PixULA_Distilled/ above
# is already ignored as the source of truth; a zip would only duplicate it).
/PixULA_Distilled.zip
